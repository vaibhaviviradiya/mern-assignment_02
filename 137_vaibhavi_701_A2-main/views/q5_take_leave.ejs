<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Take Leave</title>
    <style>
        body { font-family: Arial, sans-serif; margin: 24px; }
        form { max-width: 480px; display: grid; gap: 12px; }
        label { font-weight: 600; }
        input, select, textarea { padding: 8px; font-size: 14px; }
        button { padding: 10px 14px; font-size: 14px; cursor: pointer; }
    </style>
    <!-- If you already have a global stylesheet, remove the inline styles above -->
    <!-- Pass `employee` or `leave` data when rendering this view to prefill fields if needed -->
    <!-- Example: res.render("q5_take_leave", { employee }) -->
</head>
<body>
    <h2>Apply for Leave</h2>
    <!-- Adjust the action to match your POST route -->
    <form action="/q5/take_leave<%= token ? `?token=${token}` : '' %>" method="POST">
        <div>
            <label for="id">Employee Id</label>
            <input type="text" id="id" name="id" value="<%= (employee && employee.id) || '' %>" readonly>
        </div>

        <div>
            <label for="date">Date</label>
            <input type="date" id="date" name="date" required>
        </div>

        <div>
            <label for="reason">Reason</label>
            <textarea id="reason" name="reason" rows="4" placeholder="Enter your reason" required></textarea>
        </div>

        <button type="submit">Submit</button>
    </form>

    <p style="margin-top:16px;">
        <a href="/q5/render_data<%= token ? `?token=${token}` : '' %>">Back to Dashboard</a>
    </p>
</body>
</html>
